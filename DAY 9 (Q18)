#include <stdio.h>

int main() 
{
    int p;
    scanf("%d",&p);

    if(p>=90)
        printf("Grade A");
    else if(p>=75)
        printf("Grade B");
    else if(p>=50)
        printf("Grade C");
    else if(p>=33)
        printf("Grade D");
    else
        printf("Fail");

    return 0;
}
